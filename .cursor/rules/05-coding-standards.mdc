---
description: 
globs: 
alwaysApply: false
---
# 编码标准与规范

本项目遵循以下编码标准和规范，以确保代码质量和一致性。

## 代码注释规范

- **注释语言**: 所有代码注释必须使用英文编写，以确保国际化团队协作时的可读性。
- **文档注释**: 类、方法和主要函数应当有完整的文档注释，描述其功能、参数和返回值。
- **行内注释**: 对于复杂的逻辑，应添加适当的行内注释解释实现思路。

## 命名规范

- **类名**: 使用PascalCase命名法，如`PhysicsEngine`、`RigidBody`。
- **方法名**: 使用PascalCase命名法，如`CreateScene`、`GetRigidBody`。
- **变量名**: 成员变量使用`m_`前缀，如`m_Scene`、`m_Objects`；局部变量使用camelCase命名法。
- **常量**: 使用全大写加下划线分隔，如`MAX_OBJECTS`、`DEFAULT_GRAVITY`。

## 格式规范

- **缩进**: 使用4个空格进行缩进，不使用Tab字符。
- **括号**: 左大括号放在行尾，右大括号单独一行。
- **行宽**: 尽量保持每行不超过120个字符。
- **空行**: 使用空行分隔逻辑相关的代码块，提高可读性。

## 编码实践

- **错误处理**: 适当使用异常机制和错误码，确保异常情况被妥善处理。
- **内存管理**: 优先使用智能指针和RAII技术，避免内存泄漏。
- **性能考虑**: 对于性能关键路径的代码，应当考虑优化和度量。
- **并发安全**: 多线程代码需要考虑线程安全问题，使用适当的同步机制。

